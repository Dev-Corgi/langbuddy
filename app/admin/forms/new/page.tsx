'use client'

import { FormPageContainer } from '@/components/admin/form-page-container'

export default function NewFormPage() {
  return <FormPageContainer formId={null} pageTitle="새 신청 폼 만들기" pageTitleEn="Create New Form" />
}
